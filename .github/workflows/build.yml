name: build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: cimg/go:1.15
      options: --user 1001

    steps:
      - name: checkout code
        uses: actions/checkout@v2

      - name: build
        run: |
          go build
